extend type Query @guard {
    users: [User!]! @paginate
}

type User {
    id: ID!
    name: String!
    email: String!
    email_verified_at: String
    created_at: String
    updated_at: String
    token: String
}
